export const styles = theme => ({
  helperText: {
    paddingTop: 10,
    float: 'right',
  },
  here: {
    color: theme.palette.primary.dark,
    fontWeight: 'bold',
    cursor: 'pointer',
  },
});
