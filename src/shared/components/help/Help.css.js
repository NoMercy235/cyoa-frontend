export const styles = theme => ({
  helpBtn: {
    '&:hover': {
      cursor: 'pointer',
      color: theme.palette.primary.light,
    },
  },
  container: {
    display: 'flex',
    alignItems: 'center',
  },
});
